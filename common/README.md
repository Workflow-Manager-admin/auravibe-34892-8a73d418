# Common Utilities / Types

This folder is for code, models, or configs shared between frontend and backend.

- Example: API request/response types, constants
- Optional: Could use TypeScript if you adopt TS across codebase

Place shared modules here and import as needed from both `frontend` and `backend`.
